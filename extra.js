// const cellWidth = Math.floor(constants.BOUNDARY_WIDTH_PERC * this.canvas.width);
        // const cellHeight = Math.floor(constants.BOUNDARY_HEIGHT_PERC * this.canvas.height);
        // const playerRadiusX = Math.floor(constants.PLAYER_RADIUS_PERC * this.canvas.width);
        // const playerRadiusY = Math.floor(constants.PLAYER_RADIUS_PERC * this.canvas.height);
        // const playerVelocityX = constants.PLAYER_VELOCITY_PERC * this.canvas.width;
        // const playerVelocityY = constants.PLAYER_VELOCITY_PERC * this.canvas.height;

        